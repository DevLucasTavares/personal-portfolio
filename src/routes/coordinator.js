export const goToHomePage = (navigator) => {
  navigator('/')
}

export const goToAboutPage = (navigator) => {
  navigator('/about')
}

export const goToExperiencesPage = (navigator) => {
  navigator('/exp')
}

export const goToProjectsPage = (navigator) => {
  navigator('/projects')
}